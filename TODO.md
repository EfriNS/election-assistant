# Election Assistant - TODO

## ✅ RECENTLY COMPLETED (Last 3)

- **Fix: acronym-quote JSON truncation in follow-up/results prompts** — User reported a nonsensical follow-up option ("הרחבת סד"). Root-caused via the actual Langfuse trace: Gemini emitted a plain ASCII quote instead of gershayim inside צה"ל/סד"כ, which silently closed the JSON string early — no parse error, so the existing retry-once path never caught it, unlike the 2026-07-02/07-05 incidents. Fix replaces the "use gershayim" instruction (already in place, evidently not reliable enough) with plain-word substitutes (הצבא, משא ומתן, סדר הכוחות, תל אביב) plus a single-geresh fallback that can't break JSON regardless of glyph choice. Full detail: CHANGELOG 2026-07-10. (2026-07-10)
- **Enable Vercel Speed Insights** — Added `<SpeedInsights />` next to the existing `<Analytics />` in the root layout. Verified no CSP/header changes needed: confirmed via a production build that the script is client-injected (same-origin `/_vercel/speed-insights/*` paths, trusted transitively via the enforced CSP's `'strict-dynamic'`, same mechanism already relied on for Clarity/Analytics). Full detail: CHANGELOG 2026-07-09 (latest). (2026-07-09)
- **Fix: quota-check cron broken since CRON_SECRET was never added to Vercel; added Firewall rate-limit floor** — User reported a missing daily Slack usage message. Root-caused via git archaeology: a 2026-06-28 rename assumed Vercel auto-injects `CRON_SECRET` (false — corrected the wrong learning that caused this), leaving the auth check inert until a 2026-07-07 security fix turned it into a silent 2-day outage. Fixed: new secret added to Vercel, redeployed (env vars bake in at build time), verified 200 + Slack delivery confirmed. Also staged + user-published a blanket Vercel Firewall rule (300 req/IP/60s, all paths, currently log-only) as a volumetric floor distinct from the app's cost-control `RATE_LIMIT_RULES`. Full detail: CHANGELOG 2026-07-09 (later). (2026-07-09)

> See CHANGELOG.md for complete details.

---

## 📋 BACKLOG (Prioritized)

_Ordered by RICE thinking (reach × impact × confidence ÷ effort) — each item's rank note explains why, not a scored table. Full scope in `docs/PHASED-ROADMAP.md` where relevant._

1. **Soft launch iteration** — _Top priority: real users are on it right now, every other item waits on this._ Monitoring Langfuse, quota, and mobile behavior; iterating on live feedback. Active, ongoing (MVP phase 1.8).

2. **Content neutrality audit** (human task, not code) — _High impact — neutrality is the core value proposition, and this is the one open item that would directly validate it — but needs a 3rd party, not just dev time._ 3rd-party review of question framing. Note: a real instance of this exact risk class (dynamically-generated follow-up options one-sidedly excluding a real political position) was found and fixed 2026-07-05 via user feedback — this was AI-generated-content bias, not static question wording, but underscores that representativeness bugs are real and worth a dedicated audit, not just ad-hoc fixes as they're reported.

3. **Graphical shareable card** — _Good reach (every share action), medium effort, well-scoped, two clear implementation paths already identified._ Single-screen image (≈600×400px) optimized for social/WhatsApp sharing: top match + score, 2-3 topic chips, branding. Complements the PDF export ("share a teaser" vs. "save full results"). Options: server-side canvas (Satori/`@vercel/og`), or screenshot crop from Puppeteer reusing export-pdf infrastructure.

4. **Fix `quiz_abandoned` instrumentation gap** — _Low effort (one `beforeunload` handler), clear value, not urgent since a funnel workaround already exists._ Design doc claims it "fires on beforeunload / back navigation" but the code (`app/quiz/page.tsx:550`) only fires it from the priorities-screen back button — real mid-quiz abandonment (tab close, navigating away mid-topic) generates no event today. A funnel on `topic_completed`'s `topic_index` answers the core drop-off question without it, but a real `beforeunload` handler + per-step tracking would give direct attribution. Found while building Mixpanel dashboards (2026-07-01).

5. **Topic chip / percentage divergence** — _Affects trust on every result view (a party can show "✕" yet score 65%), but needs a design decision (a/c below) before the fix itself is small._ The chip reflects only the opener pre-calibrated score (sign); the percentage blends in AI follow-up scoring (50/50). Chip now shows its own % value inline, not just on hover (2026-07-05) — reduces but doesn't resolve the underlying divergence. Remaining options: (a) derive chip from blended topic score instead of opener, (c) leave as-is and flag for advisor review. Revisit after next user-testing round.

6. **Finalize "אודות" advisor-attribution wording** — _Tiny effort, just been sitting open._ `/about` is built and live; only the advisor-attribution placeholder still needs final wording review.

7. **Expose source-provenance/concreteness tiering to end users** — _Real reach (every grounding quote) but low confidence right now — needs a design decision and user-testing validation before it's even scoped, and risks adding density rather than reducing it._ Each grounding entry carries `provenance`/`concreteness` internally (`lib/groundings.ts`) but nothing surfaces it in the UI. Would need: an "export-grade" simplified label for end users, a decision on where it'd surface (per-quote badge? party-level note?), and validation that it helps rather than clutters. Revisit now that the broader UX/UI review concluded (2026-07-03, no redesign adopted) and micro-copy/surgical fixes are the active mode. **Partial first slice shipped 2026-07-08**: per-quote inline caption when `provenance === "official-outdated"` ("המקור הרשמי העדכני ביותר שאיתרנו... עלול שלא לשקף את עמדתה הנוכחית") plus a results-page methodology note and an `/about` methodology sentence, both stating platform text ≠ track record — driven by real user feedback (see items 8–9 below). `concreteness` and the `thirdParty`/`joint-list` tiers are still unsurfaced. **Also 2026-07-08**: `_note` (curator-only annotations — collection gaps, contradictions) is now typed (`GroundingEntry._note`/`PartyGroundings._note`) and rendered in `docs/advisor-review/grounding-review.html` (party-level callout + per-entry inline flag + a "📝 unreviewed notes" count in the stats bar) — 26 notes across all 10 parties were previously invisible even to the advisor.

8. **Sourced "documented action" counter-entry for Likud's Haredi-draft quote (stopgap)** — _Real, named user-reported trust gap (2026-07-08, screenshot feedback); narrow scope, sequenced after the item-7 staleness disclosure shipped, not blocking it._ The `haredi-draft-and-service-burden` entry (`data/groundings/likud.json`) carries a curator `_note` (now visible in the advisor review, see item 7) flagging that the 2016 constitution's universal-service language doesn't match Likud's actual coalition record on Haredi conscription. **Traced the original collection session (2026-06-23 local transcript, `~/.claude/projects/.../1e1f201d-....jsonl`) to check its evidentiary basis**: the note bundles two claims of different quality — (a) "Likud hasn't published a platform since ~2009; Netanyahu opposed publishing one, arguing the party should be judged by its actions" is attributed to an actual web-search/Wikipedia finding during that session (ironic given this exact feedback — Netanyahu himself is on record making the "judge us by actions" argument), but the specific source URL wasn't captured, so it needs re-finding before it could be cited; (b) "Likud governments have consistently exempted Haredim in practice" has **no citation at all** in that session — asserted from general knowledge, not a fetched source. Separately, the same session *did* try to source the more authoritative document for exactly this kind of claim — the 37th government's official קווי יסוד/coalition agreement (via WebSearch + WebFetch to `knesset.gov.il`/`gov.il`) — but every fetch attempt hit 403/404 and it fell back to the constitution+screenshot route instead, so no primary government document was ever captured for Likud. (For contrast: the same session *did* successfully source Shas's 2022 coalition-agreement material on the identical Haredi-draft-exemption topic, so this kind of sourcing isn't uniformly blocked — it just failed for Likud's specific document that day.) **Implication for scoping this work**: expect to need a fallback retrieval method (user-provided PDF/screenshot, alternate mirror) rather than a clean WebFetch, same as the constitution required. Source an actual verifiable record (enacted legislation text, an official Knesset vote, or a signed coalition-agreement clause — not commentary/analysis) and add it as a second, dated entry so the quote is paired with sourced counter-evidence instead of a bare curator note. Treat as a proof-of-concept for item 9 before deciding whether to generalize.

   **2026-07-08 addition — concrete leads found, plus a correction**: live-searched rather than reusing the original session's coalition-agreement angle (nothing current there) and found two better candidates: הצעת חוק שירות ביטחון תיקון 26 (Bismuth, Likud, chair Foreign Affairs & Defense Committee — not yet enacted, still pre-2nd/3rd reading) and בג"ץ 5819/24 (Supreme Court non-enforcement ruling, latest contempt order April 2026). **Correction**: the ruling looked like the stronger citation but isn't party-attributable on its own — see item 9's attribution addition for why votes beat rulings here, and use that rule before picking a final source for this entry.

9. **Design a general-purpose "documented action" grounding entry type** — _Would meaningfully extend item 7 (provenance surfacing), but feasibility is unproven — do item 8's stopgap first and use it to pressure-test the idea before committing to a schema change._ Extend `GroundingEntry`/`lib/groundings.ts` to support a second evidence class sourced from verifiable government record (legislation, votes, coalition agreements), shown alongside platform-text quotes — same verbatim-and-cited discipline as today, applied to actions instead of statements. Real feasibility risks, not just effort: (1) choosing *which* action to cite is itself an editorial judgment even when the citation is solid — risk of cherry-picking the most damning example; (2) coalition agreements/legislation are themselves a moving target (renegotiated, amended, struck down) — a "verified" counter-quote can go stale exactly as fast as the platform quote it corrects, so this relocates the staleness problem rather than retiring it, and needs its own re-verification cadence; (3) unbounded scope — once you start looking for ideology-vs-coalition-practice gaps, there may be many across 10 parties, not just this one (item 8 shows Likud alone has at least two more: LGBTQ rights, judicial-override legislation — both already flagged in `_note`, both currently invisible to the advisor). Lean toward reactive/narrow (add only when a specific gap is user-reported and the source is unambiguous), not a comprehensive sweep.

   **2026-07-08 addition — attribution is a distinct problem from sourcing, and needs its own rule**: live-checked the Likud/Haredi-draft case specifically to find a citable source, and it surfaced a real methodological gap. A Supreme Court ruling (בג"ץ 5819/24, the obvious-looking "documented action" source — the state failed to enforce conscription) is **not party-attributable on its own** — it's a finding against the executive branch as an institution; pinning it on one party requires an extra inferential step (which party led government, held which ministries) that's exactly the kind of editorial judgment this project's whole methodology exists to avoid making unilaterally. A **recorded Knesset vote** is the clean alternative — named MKs, public record, no interpretation required, the same "verbatim and cited" bar already used for platform quotes (found one for this exact bill: 63–57 advancing חוק שירות ביטחון תיקון 26 under דין הרציפות). But even that isn't a simple lookup: the vote showed real intra-coalition dissent (Defense Minister Gallant voted against his own coalition), so "look up how the party's MKs voted" doesn't reduce to "that's the party's position" — a design decision is needed on how to handle dissenting MKs (show the majority vote only? flag named dissenters? both?). **Concrete deliverables if this item is picked up**: (a) a documented rule — prefer recorded votes/named-sponsor bills over rulings or coalition agreements as the default "documented action" evidence type, specifically because votes are attributable without an inferential leap; (b) an explicit answer to the intra-party-dissent question above, before the first real entry is written; (c) a live MK-party-affiliation check at collection time (not from memory — MKs change affiliation) per the same discipline as `VAA-DESIGN.md` item 72; (d) once designed, extend `collect-party-data`'s `SKILL.md` with this as a documented sourcing playbook (what to search for, which source types rank above others, the attribution checklist) rather than re-deriving it ad hoc next time — this session's research (bill/vote/ruling leads, what failed to fetch, the attribution nuance) should be the seed content for that playbook section, not re-researched from scratch.

10. **Replace misleading quiz-completion reports on Mixpanel dashboard** — _Blocked on data: needs real post-deploy sessions with the `topics_missed` property before this is actionable, regardless of priority._ "Topic-by-topic progression" and "Selected vs. completed" both conflate "selected fewer topics" with "dropped off," since `topic_count` varies per session. `topics_missed` property already added to `quiz_completed` (deployed 2026-07-01) — once enough post-deploy sessions exist, replace both reports with one breakdown of `quiz_completed` count by `topics_missed` on dashboard `11325742`.

11. **Monitor ליכוד, ש"ס, יהדות התורה, and רע"ם for a newly-published official platform** — _Trivial recurring check, no dev effort, no urgency._ None of the 4 have an official platform (ליכוד: only the 2016 party constitution; ש"ס: `shas.org.il` now fully unresponsive, not just dead — see `VAA-DESIGN.md` item 72; יהדות התורה and רע"ם: no official site found at all). **Last checked 2026-07-08 — no change for any of the 4.** ליכוד primaries are set for 2026-08-04 — worth a re-check around then. Update `lib/parties.ts` + grounding entries if any publishes something new; `tests/partiesGroundingConsistency.test.ts` will catch a missed `platformUrl` if grounding is updated but the link isn't.

12. **Gemini paid tier: decide when to switch** — _Not yet at the usage trigger — revisit when it's actually relevant, not before._ Currently on free tier (rate-limited). Baseline: ~$0.03/session (52K tokens, 11 calls). Trigger: ~200–300 daily users (~$180–270/mo). Full analysis in `docs/API-COST-ANALYSIS.md`.

13. 💬 **DISCUSSION: Gamification option (watch — revisit if pattern grows)** — _Single data point so far — explicitly not enough signal to act on yet._ One user (R4, 20yo woman) requested Kahoot-style design: sliders, visual ranking, less text. The depth/emotional resonance is what drives the strongest positive reactions elsewhere, so this isn't a quick win. Revisit if the request appears in ≥2 more sessions.

14. **Tune critical-topic gate constants (`MAX_CRITICAL_TOPICS=2`, `GATE_SCORE_CAP=40`) with real usage** — _Single data point so far (the feedback that drove the feature) — not enough signal yet to retune, just watch._ Both are reasoned defaults, not validated against a range of real sessions. Revisit if soft-launch feedback suggests the cap is too aggressive/lenient, or if users routinely try to mark more than 2 topics קריטי.

15. **CSP rollout cleanup** — _Tiny; do it once the enforced CSP has been quiet in production for a week or two, not before._ When the `[csp-report]` server logs stay empty on production, delete the `app/api/csp-report` endpoint (a rollout safety net, not permanent infra). Also set the Clarity dashboard masking mode to "Strict" (Settings → Masking) as the backstop to the `data-clarity-mask` attribute — a human dashboard task, outside the repo.

16. **Tighten the blanket Firewall rate-limit rule from log to enforce** — _Tiny; same wait-and-observe pattern as CSP cleanup above — give it a few days of real traffic first._ The 300 req/IP/60s all-paths rule (added 2026-07-09) is currently `log`-only. Once the Firewall traffic dashboard confirms no legitimate traffic trips it, switch the rule's action from `log` to `rate_limit` (429) so it actually enforces.

17. ⏸️ **Multi-language support** — _Large scope (a full i18n layer) is why this ranks low, not the original blocker — the Hebrew MVP has been live in soft launch for over a week, so "blocked on MVP working in Hebrew" is arguably already satisfied. Revisit once the repo is public and soft launch stabilizes, not before._
    - Russian, Arabic, English UI layers; party platforms stay in Hebrew, answers/explanations translated

18. ⏸️ **Candidate records extension** — _blocked on: v1 stable (still actively iterating during soft launch)_ — experience, notable actions/votes (official sources only, no social media)

19. ⏸️ **Multi-country generalization** — _blocked on: Israel v1 validated_

---

## 📚 REFERENCE

### Key Decisions Made
- **Timeline**: ~6 months to Israeli elections
- **Audience**: General public, wide distribution (not a private tool)
- **Languages**: Hebrew-first; Russian, Arabic, English in scope
- **Data sources**: Official party platforms + candidate records; social media excluded
- **Results format**: Ranked options + "why" with exact quotations and source URLs
- **Parties**: All parties; show "no platform available" for those without one
- **Curation**: Semi-automatic ingestion + human (advisor) review/approval
- **Monetization**: Free tool; no revenue goal
- **Promotion**: Personal brand for builder; anonymous/subtle; must not hurt credibility
- **Transparency**: Open-sourced on GitHub for credibility (2026-07-06)
- **Cost model**: API-based → builder pays; must rate-limit and cap costs

### Open Questions
All resolved by what shipped: technical approach (hybrid), tone/style (formal/personal, user-selectable, live), question depth (short/deep across 9 topics), ingestion pipeline (manual `collect-party-data` skill, no admin UI needed), open-sourcing (repo went public 2026-07-06).

### Future Ideas
- Candidate profile pages (experience, voting record)
- Multi-country support (architecture to be designed with this in mind)
- Shareable results ("Here's why I'm voting for X")
- Embeddable widget for civic organizations
- "How did the parties perform on their promises?" (post-election retrospective)

### Reference Material
- Screenshots/ — Der Spiegel Hamburg election quiz (UX reference)
- README.md — Project overview, setup, tech stack
